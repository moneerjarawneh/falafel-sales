@extends('layouts.app')
@section('content')<main class="sales"><header><div><h1>نقطة البيع</h1><span>{{ auth()->user()->name }}</span></div><form method="POST" action="{{ route('logout') }}">@csrf<button class="secondary">خروج</button></form></header>@if(session('success'))<p class="notice">{{ session('success') }}</p>@endif<form method="POST" action="{{ route('sales.store') }}" id="sale-form">@csrf<section class="order"><h2>الطلب الحالي</h2><div id="cart"><p>اختر الأصناف من الأقسام أدناه.</p></div><strong>الإجمالي: <span id="total">0.00</span> د.أ</strong><button id="save" disabled>حفظ الطلب</button></section><section class="categories">@foreach($categories as $category)<article><h2>{{ $category->name }}</h2><div class="products">@foreach($category->products as $product)<button type="button" class="product" data-id="{{ $product->id }}" data-name="{{ $product->name }}" data-price="{{ $product->price }}"><b>{{ $product->name }}</b><span>{{ number_format($product->price,2) }} د.أ</span></button>@endforeach</div></article>@endforeach</section></form></main><script src="{{ asset('js/sales.js') }}"></script>@endsection
